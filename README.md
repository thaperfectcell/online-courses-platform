# 🎓 Online Courses Platform — Платформа онлайн-курсов  
*Django · PostgreSQL · Redis · Celery · Docker · Docker Compose · Docker Swarm · CI/CD*

Современная микросервисная обучающая платформа, разработанная в рамках курсовой работы по дисциплине  
**«Настройка и администрирование программного обеспечения»**.

Проект демонстрирует полный стек технологий, необходимых для построения веб-сервиса с высокой нагрузкой: контейнеризация, оркестрация, фоновая обработка задач и автоматизация развёртывания.

---

## 🚀 Основные возможности

### 🧩 Функционал платформы
- Просмотр списка курсов (с пагинацией).
- Просмотр страницы конкретного курса.
- Запись пользователя на курс.
- Отправка email-уведомления через Celery при записи.
- Авторизация и аутентификация пользователей (Django auth).
- Админ-панель Django для управления курсами и уроками.

### ⚙️ Архитектура
- **Django** — backend, шаблоны, логика приложения.
- **Gunicorn** — WSGI-сервер.
- **PostgreSQL** — основная база данных.
- **Redis** — кэш + брокер задач Celery.
- **Celery Worker** — фоновые асинхронные задачи.
- **Nginx** — реверс-прокси + отдача статических файлов.
- **Docker Compose** — локальная контейнеризация.
- **Docker Swarm** — кластерное развёртывание.
- **GitHub Actions CI/CD** — тесты → сборка → публикация → тестовый деплой.

---


## 🧪 Локальный запуск (без Docker)

### 1. Клонировать репозиторий
```bash
git clone https://github.com/<username>/online-courses-platform.git
cd online-courses-platform
```

### 2. Создать виртуальное окружение
```bash
python -m venv .venv
source .venv/bin/activate       # Linux/macOS
.venv\Scripts\activate          # Windows
```

### 3. Установить зависимости
```bash
pip install -r requirements.txt
```

### 4. Применить миграции
```bash
python manage.py migrate
```

### 5. Создать суперпользователя
```bash
python manage.py createsuperuser
```

### 6. Запустить сервер
```bash
python manage.py runserver
```

Сайт будет доступен по адресу:
👉 http://127.0.0.1:8000/

---
## 🐳 Запуск через Docker Compose
```bash
docker compose up --build
```
Будут запущены сервисы:

- web (Django + Gunicorn)

- worker (Celery)

- redis

- db (PostgreSQL)

- nginx

Доступ к приложению:
👉 http://localhost/

### 🛠️ Celery Worker

Запускается автоматически в контейнере:
```bash
celery -A config worker -l info
```

Используется для отправки email-уведомлений при записи на курс.

---

## ☁️ Развёртывание через Docker Swarm
1. Инициализация кластера
```bash
docker swarm init
```

2. Деплой
```bash
docker stack deploy -c docker-stack.yml online_courses
```

3. Проверка сервисов
```bash
docker stack services online_courses
```

4. Удаление
```bash
docker stack rm online_courses
```
---
## Эндпоинты
|Метод|Путь|Описание|Тело запроса|Ответ|
|-----|----|--------|------------|-----|
|`GET`|`/courses/`|Список курсов|-|HTML-страница со списком курсов|
|`GET`|`/courses/<id>`|Просмотреть конкретный курс и уроки|-|HTML-страница курса|
|`POST`|`/courses/<id>/enroll/`|Записаться на курс|HTML-форма вида `csrfmiddlewaretoken=...&next=/courses/1/`|Редирект обратно на страницу курса|
|`GET/POST`|`/accounts/login/`|Вход|-/Форма логина вида `csrfmiddlewaretoken=...&username=...&password=...`|Обновление сессии `Set-Cookie: sessionid=...`. Редирект обратно|
|`POST`|`accounts/logout/`|Выход|`csrfmiddlewaretoken=...`|Удаление сессии. Редирект на `courses/`|